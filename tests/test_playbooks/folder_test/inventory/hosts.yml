all:
  children:
    test:
      hosts:
        web-1-test-01.test.example.com
        db-1-test-01.test.example.com
    stage:
      hosts:
        web-1-stage-01.stage.example.com
        db-1-stage-01.stage.example.com

    prod:
      hosts:
        web-1-prod-01.prod.example.com
        db-1-prod-01.prod.example.com
  vars:
    hieradata:
      basedire: hieradata
      hierarchy:
        - common.yml
        - "environments/{{ env }}.yml"
        - "roles/{{ role }}.yml"
        - "roles/{{ role }}-{{ env }}.yml"
